class Mechanic:
    def __init__(self, name, description):
        self.name = name
        self.description = description

    def get(name):
        rows = app.db.execute(
            "SELECT "
        )

        if rows is not None else None
